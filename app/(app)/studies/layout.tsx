const StudiesLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            <h1>Studies Layout</h1>
            {children}
        </div>
    )
}

export default StudiesLayout;